<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">
    <changeSet id="create-user-table" author="Dmitrii">
        <sql>
            CREATE TABLE IF NOT EXISTS public.chats
            (
                chat_id bigserial NOT NULL ,
                message_storage_id bigint NOT NULL,
                CONSTRAINT chat_pk PRIMARY KEY (chat_id),
                CONSTRAINT gtbwebgsldhbsuhhfitbdsghuds FOREIGN KEY (message_storage_id)
                REFERENCES public.message_storage (message_storage_id) MATCH SIMPLE
                ON UPDATE NO ACTION
                ON DELETE NO ACTION
            );
        </sql>
    </changeSet>
</databaseChangeLog>